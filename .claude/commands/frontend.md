---
description: Implement or fix React dashboard changes via the frontend engineer.
argument-hint: <change description>
---

Dispatch `frontend-engineer`: $ARGUMENTS

Scope: `frontend/` — React/Vite SPA, TypeScript, Vitest tests, and `/api` integration.
`git status --short --branch` and confirm an `mr-` feature branch. Read the relevant pages,
hooks, and API client code before editing. Make minimal, focused changes. Run
`npm run typecheck`, `npm test -- --run`, and `npm run build` in `frontend/`. Hand off to
`/qa` before PR.

---
description: Implement or fix React dashboard changes via the frontend engineer.
agent: mr-frontend-engineer
---

Work on MediaRefinery frontend dashboard changes.

Scope:
- `frontend/` — React/Vite SPA, TypeScript, Vitest tests, and `/api` integration

Steps:
- Run `git status --short --branch` and confirm an `mr-` feature branch.
- Read the relevant pages, hooks, and API client code before editing.
- Make minimal, focused changes per `$ARGUMENTS`.
- Run `npm run typecheck`, `npm test -- --run`, and `npm run build` in `frontend/`.
- Hand off to `/mr-qa` before PR.

Arguments: `$ARGUMENTS`.

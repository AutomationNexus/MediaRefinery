# MediaRefinery does not use operator environment variables for configuration.
# System settings live in /data/databases/config.db (dashboard or import-yaml).
# The encryption master key is created automatically at /data/master.key on first boot.
#
# This file is kept for Compose workflows that expect a .env beside docker-compose.
# No variables are required here.
